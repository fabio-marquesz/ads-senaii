// 1-	Fazer um programa que imprima os números de 1 até um valor informado pelo usuário;

let numero;

numero = Number (prompt('Digite um numero: '));

for (let i = 0; i < numero; i++) {
    console.log(i + 1);
}